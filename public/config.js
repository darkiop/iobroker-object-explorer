// Runtime configuration — loaded before React mounts.
// In Docker: generated by docker/entrypoint.sh from env vars (IOBROKER_HOST, IOBROKER_PORT).
// In dev: served dynamically by vite.config.ts from VITE_IOBROKER_TARGET in .env.local.
window.__CONFIG__ = {
  ioBrokerHost: '',
};
